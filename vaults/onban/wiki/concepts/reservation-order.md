---
title: 예약주문
page-type: concept
domain: onban
tags: [예약주문, 온반, 주문흐름]
created: 2026-08-26
updated: 2026-08-26
related: [[onban-customer-page]], [[menu-manager-page]], [[onban-customer-manual]], [[onban-admin-manual]], [[ai-menu-recommendation]], [[onban-backend-api]], [[onban-customer-design-spec]], [[onban-admin-design-spec]]
---

# 예약주문

## Overview
오늘 판매 중인 메뉴 외에, 고객이 향후 배송받고 싶은 날짜를 지정해 마스터(창고) 메뉴 전체 중에서 미리 주문하는 기능. 핵심 설계: **예약주문 전용 상태 체계가 없다** — 일반 주문과 완전히 동일한 4단계(대기중 → 주문접수 → 배송완료 / 취소된주문)를 따르며, "예약 날짜가 지정되어 있는가"만으로 일반 주문과 구분된다.

## 고객 측 흐름 ([[onban-customer-manual]] 11장)
1. 마스터 메뉴에서 카테고리별로 1개 이상 선택 (클릭 = 체크, 재클릭 = 해제)
2. 하단 플로팅 바("선택 개수 + 📅 예약 주문 신청하기") → 팝업에서 이름/전화번호/배송주소/배송방법/예약날짜/요청사항 입력
3. **예약 날짜 범위**: 오늘로부터 1일 후 ~ 7일 이내 (단, 운영자가 등록한 "예약 이벤트"는 이벤트 기한일부터 1주일 이내로 별도 규칙 적용)
4. 신청 후 날짜 변경 불가 — 잘못 선택 시 취소 후 재신청 필요
5. 배송비 규칙은 일반 주문과 동일: 합계 2만원 미만 → 매장 픽업만 가능, 2만원 이상 + 배송 선택 → 배송비 1천원 추가

## 관리자 측 처리 ([[onban-admin-manual]] 5.5절, 6.3절)
- 주문 관리 탭의 "예약주문" 필터로 모아보기 — 상태 라벨 앞에 빨간 "예약주문 / " 접두어 (예: "예약주문 / 주문접수")
- "✅ 예약 주문 접수" 시 주문 날짜가 고객이 지정한 예약 날짜로 자동 변경
- "✅ 배송완료"는 **작업일자가 예약 날짜와 같을 때만** 활성화 — 날짜가 다르면 버튼 비활성
- **재고 차감/부족 검사가 적용되지 않는다** — 일반 주문과의 핵심 차이
- 메뉴 창고 탭의 "예약된 메뉴" 하위 탭에서 접수 완료된 예약주문만 별도 조회 가능

## 예약 이벤트 (특수 케이스)
운영자가 특정 메뉴를 기간 한정으로 홍보할 때 사용. 고객 화면(메뉴 목록 + 예약주문 페이지 상단)에 배지·설명·기한 카드로 노출되며, 클릭 시 바로 신청 팝업이 열린다. 같은 전화번호+같은 날짜 중복 신청은 서버에서 자동 거부.

## 기술 구현 (상세설계서 기준, 2026-08-26 추가)
- **판별 기준의 변천사**: 과거엔 `status==='예약주문'` 값이나 `additionalRequest` 텍스트 매칭도 함께 썼으나, 신호가 여러 개라 혼란을 유발해 **`orders.reserve_date` 컬럼(NULL 아님)이라는 단일 기준**으로 정리했다(`_isReserveOrder()`). status나 요청사항 텍스트는 판별에 전혀 관여하지 않는다.
- `reserve_date`는 접수(confirmed) 이후, 심지어 배송완료·취소 이후에도 컬럼값이 유지되므로 주문 생애주기 전체에서 예약 기원 판별이 흔들리지 않는다.
- 접수 처리(`PUT /api/orders/:id/status {status:'confirmed'}`) 시 서버가 `date` 컬럼을 `reserve_date` 값으로 자동 갱신 — 그래서 관리자 주문목록의 "작업일자 기준 조회"(`GET /api/orders?date=`)만으로는 당일 신청·접수된 예약주문이 누락되어, `createdOn=` 파라미터로 보완 조회한 뒤 병합한다. 고객 쪽도 동일한 이유로 `reservedForDevice=1` 파라미터로 보완 조회.
- **예약주문 조회 API가 2026-08에 완화됨**: `GET /api/orders?reserved=1`이 예전엔 `confirmed`만 조회해 미확정 예약주문이 화면에서 누락되는 문제가 있었음 → 취소 제외 전체 상태를 조회하도록 수정.
- 예약 이벤트 중복신청 방지는 서버가 `POST /api/orders`에서 같은 phone+reserve_date로 취소 안 된 이벤트 주문을 조회해 있으면 INSERT 전에 `409`로 거부.

## Related
AI 추천 메뉴에서 여러 개를 한 번에 예약주문으로 넘기는 흐름은 → [[ai-menu-recommendation]]
DB·API 상세: [[onban-backend-api]], [[onban-customer-design-spec]] 4.9·7.2·8.6~8.7절, [[onban-admin-design-spec]] 5.10-1절
